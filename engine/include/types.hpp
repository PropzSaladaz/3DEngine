#pragma once

#include <cstdint>

namespace mgl {
    // Default types
    using f32 = float;
    using f64 = double;
    using i32 = int;
    using u32 = unsigned;
    using u8 = std::uint8_t;
    using ui32 = std::uint32_t;
}